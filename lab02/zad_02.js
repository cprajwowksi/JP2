const array = [ 4, 2, 'a', 'b', 3, 'aa', 'ww', 2, 'ab', -2];
for (let element of array){
    for (let liczba of array){
        if (liczba === "number")
            liczba1 = liczba
    }
    for (let objekt of array){
        if (objekt !== "number")
        
    }
}